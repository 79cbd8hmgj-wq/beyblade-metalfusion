# Battle AI

Battle AI architecture is **unknown** in this evidence set. Static proximity and
conceptual profile names are insufficient to label a choice function or table.
The shared participant selector layout makes a shared resolver a **candidate**.
The reproducible next experiment is to break immediately before
`0x080300D4`, identify the AI-side writer of `+0x2C8/+0x2CC`, restore an
identical state repeatedly, and log RNG calls and outputs. No Python AI model is
provided because doing so would invent behavior.
