# Story flags and variables

No story bit is semantically named. Packed-bit helpers establish neutral byte
and bit addressing for future runtime experiments. Global versus map-local
scope, defaults, and condition operators remain **unknown**. This deliberately
retains Task 5's payload unknowns rather than deriving offsets from array sizes.
