# Rewards, inventory, shops, and unlocks

Event-facing item category, template, money, shop, and unlock operations were
searched conceptually through the named streams, but no command word has enough
consumer evidence for a stable semantic label. Their runtime and payload fields
remain **unknown**. This report does not infer IDs from Task 2/3 table sizes.
