# Creator flow foundation

## Implemented

The reference creator model contains the planned flow: player name, Bey name, avatar, skin, hair/base, hair palette, outfit palette, portrait, origin, tendency, summary, and confirmation. It supports forward, back, cancel, and bounds-sanitized choices.

## Unknown / unexecuted

The existing ROM name-entry interface and New Game hook are not confirmed, so no second keyboard or native creator hook is installed.
