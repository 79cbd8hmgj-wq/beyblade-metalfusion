# Name substitution foundation

## Implemented

The Python model performs token-aware replacement of `Tyson`/`Takao` and `TYSON`/`TAKAO` only at word boundaries, with an intentional-NPC bypass. It no longer substitutes `Gingka`, because the base ROM protagonist evidence concerns Tyson/Takao.

## Unknown / unexecuted

No native text-rendering hook is installed. The shared formatter or lookup boundary for dialogue, menus, HUD, tournament text, and result screens remains unknown.
