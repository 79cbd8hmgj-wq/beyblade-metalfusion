# Rejected or bounded battle hypotheses

* **`nested+0x0C` is HP/spin/endurance — rejected as a semantic claim.** Direct
  instructions prove mutation and signed comparisons, not the unit.
* **`+0x40` means Engine Gear — bounded to candidate.** The mask and alternate
  arithmetic are direct evidence; the diagnostic SFX proximity alone cannot
  establish the gameplay name.
* **The diagnostic tables alone prove enum storage — rejected.** The printer and
  computed selector jointly make the mapping strongly supported, pending runtime
  correlation.
* **One Bit Beast name pair is one mechanic — rejected.** The selector consumes
  runtime stats and never indexes the 32 presentation rows in its extent.
