# Rejected Task 3 hypotheses

* **confirmed rejection:** `0x000796DC..0x0007A1F4` is not one uniform table; duplicated complete-name rows transition to 32 Bit Beast/move row pairs.
* **strongly_supported rejection:** template `field_14_ptr` and `field_18_ptr` are not component masters; each is unique per complete template and has presentation-resource structure.
* **confirmed rejection:** raw template byte `0x1F` cannot be a direct Spin Gear index because values reach `0x95` while the name namespace ends at 21.
* **candidate rejected pending runtime:** nearby developer strings alone cannot name component inventory or shop functions.
